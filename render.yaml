services:
  - type: web
    name: careerscrape
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate --noinput
      python manage.py collectstatic --noinput
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 2 mysite.wsgi
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: careerscrape.com,www.careerscrape.com,careerscrape.onrender.com
      - key: CSRF_TRUSTED_ORIGINS
        value: https://careerscrape.com,https://www.careerscrape.com,https://careerscrape.onrender.com
      - key: DATABASE_URL
        sync: false
